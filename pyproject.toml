[project]
name = "python"
version = "0.1.0"
description = "Seabhac.io api client in python"
requires-python = ">=3.13"
dependencies = [
    "pytest>=9.0.3",
    "requests>=2.33.1",
    "responses>=0.26.0",
]
